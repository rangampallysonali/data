pipeline {
    agent { label 'docker-agent' }
    stages {
        stage('install dependencies') {
    steps {
      bat'''
          
            pip3 install -r requirements.txt
        '''
    }
}       
       stage('Run The code') {
    steps {
       bat ''' 
            python main.py
        '''
    }
}
      
    }
}
